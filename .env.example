# Server
PORT=3000

# CORS: allowed frontend origin. Fallback: http://localhost:3001
FRONTEND_URL=http://localhost:3001

# VAPI
VAPI_API_KEY=your-vapi-api-key
# Base URL for webhooks (e.g. ngrok tunnel). Example:
VAPI_BASE_URL=https://your-subdomain.ngrok-free.app
# Optional: existing assistant ID; if unset, one is created on first session start
# VAPI_ASSISTANT_ID=
